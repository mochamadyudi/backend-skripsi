import TransactionOrderService from "./transaction-order.service";

export default class TransactionOrderModule extends TransactionOrderService {
    constructor(props = {}) {
        super(props)
    }
}